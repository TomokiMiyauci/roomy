import { shallowMount } from '@vue/test-utils'

import RecordingStudio from '@/components/molecules/RecordingStudio.vue'

describe('RecordingStudio.vue', () => {
  it('should be ', () => {
    const wrapper = shallowMount(RecordingStudio)
    expect(wrapper)
  })
})
