import { shallowMount } from '@vue/test-utils'
import ButtonImage from '@/components/molecules/ButtonImage.vue'

describe('ButtonImage.vue', () => {
  it('should be ', () => {
    const wrapper = shallowMount(ButtonImage)
    expect(wrapper)
  })
})
