import { shallowMount } from '@vue/test-utils'

import ThePost from '@/components/organisms/ThePost.vue'

describe('ThePost.vue', () => {
  it('should be ', () => {
    const wrapper = shallowMount(ThePost)
    expect(wrapper)
  })
})
