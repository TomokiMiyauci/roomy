import BaseIcon from '@/components/atoms/BaseIcon.vue'

export default {
  title: `atoms|BaseIcon`,
  parameters: {
    info: {
      summary: `atoms of BaseIcon is great component.`
    }
  }
}

const template = ``

export const Default = () => ({
  components: { BaseIcon },
  template
})
